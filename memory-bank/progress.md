# Progress

## Working

- Модуль `github.com/alkulagin-creator/tt` собирается (`go build ./...`, код 0).
  Пакетов с кодом пока нет — сборка проходит с предупреждением
  `matched no packages`, это ожидаемо для каркаса.

## Deployed

Ничего: инструмент локальный, «деплой» — это сборка `tt.exe` на машине владельца.
Первый пригодный к запуску бинарник появится на TT-009 (`tt list`).

## In progress

- **TT-001** — установка Go и каркас модуля. Осталось закоммитить.

## Next

- **TT-002, TT-003, TT-005, TT-007** — открываются сразу после TT-001, независимы,
  делаются параллельно.
- **TT-006** — писатель с гарантией «diff ровно одна строка». Самая ответственная задача
  этапа: на ней держатся все остальные записи.
- **TT-013** — приёмка на живом vault из 1251 таски. `tt doctor --fix` на живом vault
  запускается **только после явного согласия владельца**; сначала проверка на копии.

## Не сделано и почему

- `index` с `fsnotify` — этап 3: до сервера у снимка в памяти нет потребителя.
- `tt new`, `tt claim`, `tt done` — этап 4, вместе с переводом слеш-команд.
- Удаление `kanban/`, вотчера и bash-скриптов — этап 2 и 4, после появления замены.
