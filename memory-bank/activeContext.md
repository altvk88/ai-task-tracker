# Active Context

## Current focus

Этап 1 (ядро и CLI) закрыт полностью: TT-001…TT-014, четырнадцать задач.
Открытых работ нет — ждём решений владельца по двум вопросам, см. «Next steps».

Спек: `D:/task tracker/docs/superpowers/specs/2026-08-31-local-task-tracker-ui-design.md`
План этапа 1: `D:/task tracker/docs/superpowers/plans/2026-08-31-tt-core-phase-1.md`

## Recent changes

Всё — 2026-08-31.

- Ядро целиком: `model`, `vault`, `cli`, команды `list` / `set` / `doctor` / `doctor --fix`.
- `.task-tracker/schema.json` положен в vault — общий контракт для будущих плагина и доски.
- `model` научен читать исторические формы `verify` и `claim` (TT-014): неразбираемых
  тасок стало 69 вместо 553.
- Постоянный смоук-тест по живому vault (`TT_SMOKE_VAULT`), нашедший эту проблему.
- Зафиксирована семантика замка: признак «таска в работе», а не мьютекс команды.
- Из правил, плана и спека убрано требование `-race` — на машине он невозможен.
- Цифры спека приведены к измеренным: бинарник 5.0 МБ, обход 1266 тасок ~125 мс.

Живой vault за весь этап не изменён ни одной командой `tt`: всё проверялось на копиях.

## Next steps

Два решения за владельцем, работа без них не начинается:

1. **Запускать ли `tt doctor --fix` по живому vault.** На копии: 93 починки —
   81 залипший `claim`, 8 осиротевших замков, 4 закавычивания. Проверено, что меняется
   ровно 85 файлов и ни одного лишнего. Отдельно учесть: в vault параллельно пишет другая
   сессия (проект `webapp`), массовую правку стоит делать, когда там тихо.
2. **TT-015 — как чинить 37 тасок с пропавшим закрывающим фенсом.** Автоматически
   (угадывая границу фронтматтера) или списком, по одному. Автоматика соблазнительна,
   но именно угадывание один раз уже стоило этому vault 37 файлов.

Дальше по спеку — этап 2: плагин Obsidian, и только после него снос `kanban/`, вотчера,
VBS-лаунчера и PostToolUse-хука.
