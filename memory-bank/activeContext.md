# Active Context

## Current focus

Репозиторий опубликован (`https://github.com/altvk88/ai-task-tracker`), история
вычищена от внутренних имён и путей владельца — при любой правке текстов/скриптов
держаться условных имён проектов (`webapp`, `crm`, ...) и не заносить их обратно.

TT-057 (установщик macOS) и TT-058 (инструкция по подключению агентов) — см.
`progress.md`. TT-047 в `needs-input`: установщик Windows собран, но три проверки
(реальный PATH, автозапуск при входе, клик по ярлыку) выполнимы только на машине
владельца. Остальное закрыто: настройки, scaffold, ярлыки, иконка, рендер markdown,
правка и создание тасок с доски.

Спек: `D:/task tracker/docs/superpowers/specs/2026-08-31-local-task-tracker-ui-design.md`
Планы этапов лежат рядом, в `docs/superpowers/plans/`.

## Recent changes

Всё за 2026-08-31 и 2026-09-01.

- **Этап 1** — ядро: `model`, `vault`, `taskop`, `cli`; `list`, `set`, `doctor`.
- **Этап 2** — плагин Obsidian: канбан из `metadataCache`, перетаскивание, запись
  через `processFrontMatter`.
- **Этап 3** — сервер: индекс в памяти с `fsnotify`, HTTP, SSE, веб-доска на Svelte,
  токен на запись из локальной сети.
- **Этап 4** — `new`, `next`, `claim`, `release`, `reset`, `done`; слеш-команды всех
  четырёх семейств агентов переведены на `tt`; замещённые bash-скрипты сняты.
- **Данные** — vault почищен: 179 проблем стало 49, неразбираемых тасок 553 стало 31.
- Файловые Kanban-доски, вотчер, VBS-лаунчер и PostToolUse-хук удалены. Копии всего
  удалённого лежат в `attic/` под git.

## Next steps

Три действия ждут владельца:

0. **Проверить установщик на живой системе**: `installer/Output/tt-setup-1.0.0.exe` — после
   установки новый терминал должен видеть `tt list` без `--vault`, а перезаход в систему —
   поднимать доску без окна консоли. Тогда TT-047 закрывается.

1. **`D:/tt` не имеет remote** — вся история в одном экземпляре на диске. Нужно решить,
   заводить ли удалённый репозиторий.
2. **Коммит с плагином в `D:/task-tracker-agent` не запушен** — публикация в общий
   репозиторий команды остаётся за владельцем. Там же непроверенным остался один шаг
   инструкции: включить плагин в Obsidian и увидеть доску, из терминала это невозможно.

Возможные направления, ни одно не заведено задачей: подкоманда `tt` взамен
`tt-resolve.sh`, аналог `tt-active.sh` для statusline, приведение раздаваемой версии
к состоянию после перехода на `tt`.
